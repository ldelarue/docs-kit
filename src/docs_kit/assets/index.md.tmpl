# {title}

{description}

This site is generated and maintained with **docs-kit**: the API reference is
rendered from `openapi.json` (`mise run docs:refresh`), everything else is
hand-written Diátaxis content.

- **[API reference](references/api.md)** — interactive Swagger UI plus
  generated endpoint tables.
- **Tutorials / Guides / Explanation** — write these by hand, one Diátaxis
  type per page.
